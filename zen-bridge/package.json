{
  "name": "zen-bridge",
  "version": "0.1.0",
  "private": true,
  "description": "HTTP sidecar that connects to a local opencode serve instance.",
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "express": "^4.19.2"
  },
  "engines": {
    "node": ">=20"
  }
}