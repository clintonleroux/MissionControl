{
  "name": "zen-bridge",
  "version": "0.1.0",
  "private": true,
  "description": "HTTP sidecar exposing the Opencode SDK to the Mission Control C# app.",
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "@opencode-ai/sdk": "latest",
    "express": "^4.19.2"
  },
  "engines": {
    "node": ">=20"
  }
}
